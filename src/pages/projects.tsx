import React from 'react';
import Projects from '../components/Projects'; // Adjust the path based on your structure

const ProjectsPage = () => {
  return (
    <main className="min-h-screen bg-gray-900 text-white">
      <Projects />
    </main>
  );
};

export default ProjectsPage;